# Release 92 - Warranty release

Adds `/api/payments/release-warranty` and aliases. A succeeded SetupIntent is retained for audit; the reusable PaymentMethod is detached and the booking is marked released.
